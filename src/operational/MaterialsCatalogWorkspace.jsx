import React,{useState}from"react";
import{Button,Field,Notice,Panel,friendlyError,inputStyle}from"./ui";
export function MaterialsCatalogWorkspace({data,insertRow}){const[name,setName]=useState("");const[unit,setUnit]=useState("قطعة");const[error,setError]=useState("");const[ok,setOk]=useState("");async function add(){setError("");setOk("");if(!name.trim())return setError("اكتب اسم المادة");const e=await insertRow("materials",{name:name.trim(),unit,unit_cost:0,initial_stock:0});if(e)return setError(friendlyError(e));setName("");setOk("تم إنشاء تعريف المادة. أضف الرصيد من دورة المشتريات والاستلام فقط.")}return <div><h2>دليل المواد الخام</h2><Notice>تم إيقاف تسجيل شراء الخامة المباشر. زيادة الرصيد تتم فقط من: طلب شراء ← أمر شراء ← استلام مخزني.</Notice>{error&&<Notice type="error">{error}</Notice>}{ok&&<Notice>{ok}</Notice>}<Panel title="إضافة تعريف مادة"><div style={{display:"flex",gap:10,flexWrap:"wrap",alignItems:"end"}}><Field label="اسم المادة"><input style={inputStyle} value={name} onChange={e=>setName(e.target.value)}/></Field><Field label="الوحدة"><input style={inputStyle} value={unit} onChange={e=>setUnit(e.target.value)}/></Field><Button onClick={add}>إضافة</Button></div></Panel><Panel title="المواد المعرفة"><div style={{display:"grid",gap:8}}>{data.materials.map(m=><div key={m.id} style={{padding:10,border:"1px solid var(--color-border)",borderRadius:9}}><strong>{m.name}</strong> — {m.unit||"وحدة"}</div>)}{!data.materials.length&&<span>لا توجد مواد معرفة.</span>}</div></Panel></div>}
