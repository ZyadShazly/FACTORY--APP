import React,{useEffect,useMemo,useState}from"react";
import{supabase}from"../supabaseClient";
import{Button,Notice,Panel,friendlyError,money}from"./ui";
export function InventoryWorkspace({canViewFinancials=true}){const[workspace,setWorkspace]=useState({items:[],balances:[],warehouses:[],movements:[]});const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[search,setSearch]=useState("");async function load(){setLoading(true);setError("");const{data,error}=await supabase.rpc("get_inventory_workspace");if(error)setError(friendlyError(error));else setWorkspace(data||{items:[],balances:[],warehouses:[],movements:[]});setLoading(false)}useEffect(()=>{void load()},[]);const balances=useMemo(()=>workspace.balances.filter(r=>`${r.item_name} ${r.sku} ${r.warehouse_name}`.toLowerCase().includes(search.toLowerCase())),[workspace,search]);const totals=useMemo(()=>workspace.balances.reduce((a,r)=>({qty:a.qty+Number(r.quantity_on_hand||0),value:a.value+Number(r.inventory_value||0)}),{qty:0,value:0}),[workspace]);return <div><h2>المخزون الفعلي</h2><p style={{color:"var(--color-text-muted)"}}>الأرصدة أدناه من دفتر حركات المخزون المعتمد، وليس من حسابات يدوية.</p>{error&&<Notice type="error">{error}</Notice>}{loading?<Notice>جارِ تحميل المخزون...</Notice>:<><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(180px,1fr))",gap:12,marginBottom:16}}><Panel title="الأصناف النشطة"><strong style={{fontSize:24}}>{workspace.items.length}</strong></Panel><Panel title="إجمالي الكمية"><strong style={{fontSize:24}}>{money(totals.qty)}</strong></Panel>{canViewFinancials&&<Panel title="قيمة المخزون"><strong style={{fontSize:24}}>{money(totals.value)}</strong></Panel>}</div><Panel title="الأرصدة" actions={<Button tone="ghost" onClick={load}>تحديث</Button>}><input style={{width:"100%",marginBottom:12,padding:10,border:"1px solid var(--color-border)",borderRadius:9}} value={search} onChange={e=>setSearch(e.target.value)} placeholder="ابحث باسم الصنف أو المخزن..."/><div style={{overflowX:"auto"}}><table style={{width:"100%",borderCollapse:"collapse"}}><thead><tr>{["الصنف","المخزن","الوحدة","الرصيد",...(canViewFinancials?["متوسط التكلفة","القيمة"]:[])].map(h=><th key={h} style={{textAlign:"right",padding:9,borderBottom:"1px solid var(--color-border)"}}>{h}</th>)}</tr></thead><tbody>{balances.map(r=><tr key={`${r.inventory_item_id}-${r.warehouse_id}`}><td style={{padding:9}}>{r.item_name}</td><td>{r.warehouse_name}</td><td>{r.unit}</td><td>{money(r.quantity_on_hand)}</td>{canViewFinancials&&<><td>{money(r.average_unit_cost)}</td><td>{money(r.inventory_value)}</td></>}</tr>)}{!balances.length&&<tr><td colSpan="6" style={{padding:16,textAlign:"center"}}>لا توجد أرصدة مطابقة.</td></tr>}</tbody></table></div></Panel><Panel title="آخر حركات المخزون"><div style={{display:"grid",gap:8}}>{workspace.movements.slice(0,20).map(m=><div key={m.id} style={{padding:10,border:"1px solid var(--color-border)",borderRadius:9,display:"flex",justifyContent:"space-between",gap:10}}><span><strong>{m.item_name}</strong> — {m.warehouse_name}</span><span>{money(m.quantity_delta)}</span></div>)}{!workspace.movements.length&&<span>لا توجد حركات مخزون بعد.</span>}</div></Panel></>}</div>}
